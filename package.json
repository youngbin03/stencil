{
  "name": "stencil",
  "version": "0.0.0",
  "private": true,
  "description": "SVG template based AI presentation generation system",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --build --noEmit",
    "clean": "tsc --build --clean"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
